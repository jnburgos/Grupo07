<%= form_for(show_subscription) do |f| %>
  <% if show_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(show_subscription.errors.count, "error") %> prohibited this show_subscription from being saved:</h2>

      <ul>
      <% show_subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :show %>
    <%= f.text_field :show %>
  </div>

  <div class="field">
    <%= f.label :user %>
    <%= f.text_field :user %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
